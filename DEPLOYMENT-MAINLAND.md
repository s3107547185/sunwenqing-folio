# 中国大陆部署说明

## 本次适配

- 移除了 Google Fonts 外部请求，断开海外字体服务时页面仍可正常渲染。
- 首屏视频改为 `metadata` 预加载。
- 项目视频改为接近视口时才加载，避免手机打开页面时同时请求约 39MB 视频。
- 未重新编码或修改任何视频素材。

## 构建

```bash
npm install
npm run build
```

将 `dist/` 内的全部文件上传到中国大陆可访问的静态托管或 CDN。

## 域名与备案

不要继续把 Surge 子域名作为大陆用户的唯一入口。建议使用自己的域名，并按照所选云厂商要求完成域名备案、解析和 HTTPS 配置。

项目使用根路径资源，例如 `/assets/video/work-01.mp4`，部署到域名根目录即可，无需修改 Vite 的 `base`。
